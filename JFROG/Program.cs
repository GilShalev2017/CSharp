using System;
using System.Collections.Generic;
/*
class Program
{
    static void Main()
    {
        // Create an array of lists
        List<int>[] arrayOfLists = new List<int>[5];

        // Initialize each list in the array
        for (int i = 0; i < arrayOfLists.Length; i++)
        {
            arrayOfLists[i] = new List<int>();
        }

        // Add elements to the lists
        arrayOfLists[0].Add(10);
        arrayOfLists[1].Add(20);
        arrayOfLists[1].Add(25);
        arrayOfLists[3].Add(30);
        arrayOfLists[3].Add(35);
        arrayOfLists[3].Add(40);

        // Access elements in the array of lists
        Console.WriteLine("Elements in the array of lists:");
        for (int i = 0; i < arrayOfLists.Length; i++)
        {
            Console.Write($"List {i}: ");
            foreach (int value in arrayOfLists[i])
            {
                Console.Write(value + " ");
            }
            Console.WriteLine();
        }
    }
}
*/